{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "clean": "node -e \"const fs=require('fs'); const path=require('path'); const distPath=path.join(__dirname,'dist'); if(fs.existsSync(distPath)) fs.rmSync(distPath, {recursive:true, force:true}); fs.mkdirSync(distPath, {recursive:true});\"",
    "clean:frontend:js": "node -e \"const fs=require('fs'); const path=require('path'); function cleanJS(dir){try{const files=fs.readdirSync(dir); files.forEach(file=>{const fullPath=path.join(dir,file); if(fs.statSync(fullPath).isDirectory()){cleanJS(fullPath)}else if(file.endsWith('.js')){fs.unlinkSync(fullPath)}})}catch(e){}} cleanJS(path.join(__dirname,'..','frontend'));\"",
    "build": "npm run clean && npm run clean:frontend:js && tsc && npm run build:frontend && npx copyfiles -u 2 \"../frontend/**/*\" ./dist/frontend --exclude \"../frontend/public/**\" --exclude \"../frontend/node_modules/**\" --exclude \"../frontend/**/*.js\"",
    "prebuild:frontend": "cd ../frontend && npm install typescript --no-save",
    "build:frontend": "cd ../frontend && npx tsc",
    "build:all": "npm run build",
    "dev": "nodemon",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "multer": "^2.0.1",
    "nodemailer": "^7.0.3",
    "papaparse": "^5.5.3",
    "pdfkit": "^0.17.1",
    "pg": "^8.16.2",
    "puppeteer": "^24.10.2",
    "typescript": "^5.8.3",
    "xml-js": "^1.6.11"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/multer": "^1.4.13",
    "@types/node": "^20.12.12",
    "@types/nodemailer": "^6.4.17",
    "@types/papaparse": "^5.3.16",
    "@types/pdfkit": "^0.14.0",
    "@types/pg": "^8.15.4",
    "@types/puppeteer": "^5.4.7",
    "copyfiles": "^2.4.1",
    "glob": "^11.0.3",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2"
  }
}